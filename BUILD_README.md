# 打包说明

本项目提供了自动打包脚本，用于将程序打包成单个可执行文件，确保包含必要的资源文件（如fonts目录中的字体文件）。

## 打包前准备

1. 确保已安装 Python 3.7 或更高版本
2. 确保项目文件结构完整，尤其是以下关键文件/目录：
   - 主要入口文件 (main.py 或 app.py)
   - fonts/ 目录 (包含字体文件)
   - 各模块文件 (如 account_manager.py, video_analyzer.py 等)
   - logo.ico (程序图标)

## Windows 用户

1. 双击执行 `build.bat` 文件即可开始打包过程。
2. 如果遇到权限问题，请右键点击 `build.bat` 文件，选择"以管理员身份运行"。

## Linux/MacOS 用户

1. 首先给脚本添加执行权限:
   ```bash
   chmod +x build.sh
   ```

2. 然后执行脚本:
   ```bash
   ./build.sh
   ```

3. 如果遇到权限问题，请使用sudo:
   ```bash
   sudo ./build.sh
   ```

## 手动打包

也可以直接运行 Python 脚本进行打包:

```bash
# Windows
python build_dir.py

# Linux/MacOS
python3 build_dir.py
```

## 打包过程

打包脚本会自动执行以下操作：

1. 安装必要的依赖库 (如 PyInstaller)
2. 清理之前的构建文件
3. 检测合适的入口文件 (main.py 或 app.py)
4. 收集所有需要的资源文件 (fonts、assets、ui 等)
5. 创建运行时钩子，确保可执行文件能正确加载资源
6. 执行 PyInstaller 打包
7. 复制生成的可执行文件到项目根目录

## 打包结果

打包完成后，可执行文件将位于:
1. `dist` 目录下
2. 项目根目录下 (会自动复制一份)

文件名为 `支付宝上传和分析工具.exe` (Windows) 或 `支付宝上传和分析工具` (Linux/MacOS)

## 常见问题解决

### 打包失败

1. **ModuleNotFoundError**：
   - 问题：打包时提示找不到某个模块
   - 解决：手动安装缺失的模块 `pip install <模块名>`
   - 然后编辑 `build_dir.py` 文件，在 `隐藏导入` 列表中添加该模块

2. **找不到入口文件**：
   - 问题：提示无法找到 main.py 或 app.py
   - 解决：确认项目中存在这些文件，或编辑脚本指定正确的入口文件

3. **PyInstaller 版本问题**：
   - 问题：PyInstaller 相关错误
   - 解决：尝试安装特定版本 `pip install pyinstaller==5.13.0`

### 运行时问题

1. **缺少DLL文件**：
   - 问题：运行时提示缺少某些DLL文件
   - 解决：安装 Microsoft Visual C++ Redistributable

2. **无法找到资源文件**：
   - 问题：程序启动但无法加载字体或其他资源
   - 解决：确保打包时包含了所有必要的资源目录

3. **权限问题**：
   - 问题：程序无法写入文件或访问资源
   - 解决：以管理员身份运行程序，或将程序安装在有适当权限的目录

## 运行打包后的程序

打包成功后，可以直接双击可执行文件运行程序。程序会自动使用打包进去的资源，包括fonts目录中的字体文件和其他必要资源。

如果程序需要配置文件，请确保将必要的配置文件放在可执行文件所在的目录中。 